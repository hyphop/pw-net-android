<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- black background -->
  <item android:drawable="@color/ic_mn_bg"/>
  <!-- cyan triangle, inset so it isn’t edge-to-edge -->
  <item
      android:drawable="@drawable/ic_mn_foreground"
      android:left="12dp"
      android:top="12dp"
      android:right="12dp"
      android:bottom="12dp"/>
</layer-list>
